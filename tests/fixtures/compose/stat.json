[
    {
        "name": "default — wrapped=true, no color, no trend",
        "props": {},
        "expected": {
            "root": "rounded-[var(--radius-box)] tune-border border-base-content/10 bg-base-100 shadow-[var(--shadow-box)] overflow-hidden",
            "inner": "flex items-center justify-between gap-4 px-6 py-4",
            "text": "flex flex-col gap-1 min-w-0",
            "figure": "shrink-0 flex items-center justify-center text-base-content/60",
            "title": "text-xs text-base-content/60 whitespace-nowrap",
            "value": "text-3xl font-extrabold whitespace-nowrap text-base-content",
            "desc": "text-xs whitespace-nowrap text-base-content/60"
        }
    },
    {
        "name": "+ desc — no trend, base desc class only",
        "props": { "desc": "since last month" },
        "expected": {
            "root": "rounded-[var(--radius-box)] tune-border border-base-content/10 bg-base-100 shadow-[var(--shadow-box)] overflow-hidden",
            "value": "text-3xl font-extrabold whitespace-nowrap text-base-content",
            "desc": "text-xs whitespace-nowrap text-base-content/60"
        }
    },
    {
        "name": "+ valueColor=primary — value gets text-primary, figure tracks color",
        "props": { "valueColor": "primary" },
        "expected": {
            "value": "text-3xl font-extrabold whitespace-nowrap text-primary",
            "figure": "shrink-0 flex items-center justify-center text-primary"
        }
    },
    {
        "name": "+ trend=up + trendValue — desc text-success (arrow lives in blade helper)",
        "props": { "trend": "up", "trendValue": "+12%" },
        "expected": {
            "desc": "text-xs whitespace-nowrap text-success"
        }
    },
    {
        "name": "+ trend=down + trendValue — desc text-error",
        "props": { "trend": "down", "trendValue": "-3%" },
        "expected": {
            "desc": "text-xs whitespace-nowrap text-error"
        }
    },
    {
        "name": "trend=flat — desc text-base-content/50",
        "props": { "trend": "flat" },
        "expected": {
            "desc": "text-xs whitespace-nowrap text-base-content/50"
        }
    },
    {
        "name": "wrapped=false — root empty (no outer card chrome)",
        "props": { "wrapped": false },
        "expected": {
            "root": "",
            "inner": "flex items-center justify-between gap-4 px-6 py-4"
        }
    },
    {
        "name": "all props — valueColor=success + trend=up + trendValue + desc",
        "props": {
            "valueColor": "success",
            "trend": "up",
            "trendValue": "+24%",
            "desc": "vs last week"
        },
        "expected": {
            "root": "rounded-[var(--radius-box)] tune-border border-base-content/10 bg-base-100 shadow-[var(--shadow-box)] overflow-hidden",
            "value": "text-3xl font-extrabold whitespace-nowrap text-success",
            "figure": "shrink-0 flex items-center justify-center text-success",
            "desc": "text-xs whitespace-nowrap text-success"
        }
    },
    {
        "name": "valueColor=error — error tone on value/figure",
        "props": { "valueColor": "error" },
        "expected": {
            "value": "text-3xl font-extrabold whitespace-nowrap text-error",
            "figure": "shrink-0 flex items-center justify-center text-error"
        }
    },
    {
        "name": "valueColor=secondary + wrapped=false — inner only, no root wrapper",
        "props": { "valueColor": "secondary", "wrapped": false },
        "expected": {
            "root": "",
            "inner": "flex items-center justify-between gap-4 px-6 py-4",
            "value": "text-3xl font-extrabold whitespace-nowrap text-secondary",
            "figure": "shrink-0 flex items-center justify-center text-secondary"
        }
    }
]
